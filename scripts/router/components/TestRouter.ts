import $$TestRouter from 'generated/router/components/TestRouter';

export default class TestRouter extends $$TestRouter {
  constructor(basePath: string, name: string, routeIndex: number) {
    super(basePath);
  }
}
